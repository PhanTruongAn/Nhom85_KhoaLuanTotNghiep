import { createTheme } from "@mui/material/styles";
const themeLight = createTheme({
  palette: {
    mode: "light",
    secondary: {
      main: "#e19d34",
      dark: "#d8831d",
      light: "#e5ad54",
    },
  },
});
export default themeLight;
